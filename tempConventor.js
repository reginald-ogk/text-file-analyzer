// Converts Celsius to Fahrenheit
// Usage: convertCtoF(0) returns 32
// Usage: convertCtoF(100) returns 212

function convertCtoF(temperature) {
  return temperature * (9/5) + 32
}

// formula degrees F = degrees C × 9/5 + 32